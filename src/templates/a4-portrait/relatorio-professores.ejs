<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="http://localhost:2000/assets/css/font.css">
    <%- include("../css/ficha.ejs") %>
    <%- include("../css/common.ejs") %> 
    <style>
         #titulo{
            font-size: 20px;
            text-align: center;
            margin-bottom: 10px;
        }
    </style>
</head>
<body>
    <div id="container">
        <%- include("../reusable/header-escola.ejs") %>
        <div style="margin-top: 40px;">
            <div id="titulo" ><b>Relatório de Professores</b></div>
            <div style="display: flex;">
                <table class="table" style="width: 100%; border-spacing: 0;">
                    <tr>
                        <th>ID da InfoEduc</th>
                        <th>Nome do professor</th>
                        <th>Turma</td>
                        <th>Celular</td>
                    </tr>
                    <% lista_professor.forEach(element => { %>
                        <tr>
                            <td style="width: 20%;"><%= element.id_infoeduc %></td>
                            <td style="width: 40%;"><%= element.nome_professor %> <%= element.sobrenome_professor %> </td>
                            <td style="width: 20%;">
                                <% lista_turmas.forEach(turmas => { %>
                                    <% if (turmas.id_professor == element.id_professor) { %>
                                        <%= turmas.nome_turma %>;
                                    <% } %>
                                <% }) %>
                            </td>
                            <td style="width: 15%;"><%= element.celular %> </td>
                        </tr>
                    <% }) %>
                </table>
            </div>
        </div>
    </div>
</body>
</html>